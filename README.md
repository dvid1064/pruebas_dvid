#Ejecución del Notebook Reto BP

#1. Una vez realizado el paso de instalación de Spark y SQLLite considerar si existe un problema la instalación de Java.
#Este punto requiere la ejecución de la celda **Instalacion de Java 17 para corrgeir el error**

#2. Las celdas **Verifica si esta correcto PySpark** y ejecuta la verificación de instalación de Spark y SQLite.
#Si existe un problema Reiniciar la sesión **Se requiere el El reinicio de Entorno, dando click en Entorno de ejecución y seleccionando Reiniciar Sesión**

#3. En la ejecución del punto **5.1 Preparar un programa de pyspark que permita leer el archivo "CENTROS_EDUCACION_MADRID.json".**
#Considerar antes de ejecutar el proceso se debe cargar el archivo en la carpeta Content, dando click en el icono de Carpeta a la izquierda y ubicandose
#sobre la carpeta content, click derecho y se elige Subir y se carga el archivo CENTROS_EDUCATIVOS_MADRID.json
